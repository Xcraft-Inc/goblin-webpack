{
  "pack": {
    "parallel": true,
    "desc": "Pack a webapp",
    "options": {
      "params": {
        "required": "location"
      }
    }
  },
  "server.start": {
    "parallel": true,
    "desc": "Start a webpack server",
    "options": {
      "params": {
        "required": "goblin"
      }
    }
  },
  "server.stop": {
    "parallel": true,
    "desc": "Stop a webpack server",
    "options": {
      "params": {
        "required": "goblin"
      }
    }
  },
  "_save.stats": {
    "parallel": true,
    "desc": null,
    "options": {
      "params": {
        "required": "goblin",
        "optional": "stats"
      }
    }
  }
}